[project]
name = "becorelab-mcp"
version = "1.0.0"
description = "비코어랩 MCP 서버 — Claude 앱에서 사무실 API 호출"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
