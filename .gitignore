# 로그 파일
*.log

# 스크린샷 (업로드 결과 등)
*.png
!sourcing/analyzer/static/team/*.png

# macOS
.DS_Store

# 설정 파일 (비밀번호 포함 — 절대 커밋 금지)
rocket_config.py
rocket/rocket_config.py

# DB (로컬 데이터)
*.db
instagram/instagram.db

# Chrome 프로필 (쿠키/세션 — 절대 커밋 금지)
sourcing/alibaba_profile/
instagram/session/

# 실행 결과물 (로컬 전용)
sourcing/output/

# 인스타그램 세션 (로그인 정보 — 절대 커밋 금지)
instagram/session.json
instagram/api_session.json

# 엑셀/데이터 파일
*.xls
*.xlsx
*.csv

# 환경변수 (API 키 — 절대 커밋 금지)
.env
sourcing/analyzer/.env

# Firebase 서비스 계정 키 (절대 커밋 금지)
*-firebase-adminsdk-*.json

# 텔레그램/디스코드 봇 (토큰 하드코딩 — 절대 커밋 금지)
pixie-bot/
lena-bot/
discord-bots/config.py
discord-bots/group_chats.json
discord-bots/*.log

# 봇 공유 데이터 (로컬 전용)
data/

# Node modules
**/node_modules/
accounting/package-lock.json
accounting/package.json

# Python
__pycache__/
*.pyc
*.pyo

# Claude Code 로컬 설정 (봇 토큰 등 민감 정보 포함 — 절대 커밋 금지)
**/.claude/settings.local.json

# 레나 런처 (봇 토큰 하드코딩 — 절대 커밋 금지)
Channel_lena/run_lena.bat
